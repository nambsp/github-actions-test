name: Run on code-default
on:
  push:
    branches: [ master ]

jobs:
  run-shell-command:
    runs-on: [ code-medium ] 
    steps: 
        - name: echo a string
          run: echo " Hello world1"
        - name: echo multiline script
          run: | 
            node -v
            npm -v